export default function NotFound() {
    return (
        <div className="h-[80vh] w-full flex items-center justify-center font-mono relative overflow-hidden">
            not found
        </div>
    );
}
